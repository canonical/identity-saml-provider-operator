name: Release-please

on:
  workflow_dispatch:
  push:
    branches:
    - main
    - "track/**"

permissions:
    contents: write
    pull-requests: write

jobs:
  release:
      name: release
      uses: canonical/identity-team/.github/workflows/charm-release.yaml@be2569198bf90722de218a28abce81786472b83a # v1.16.1
      secrets:
        PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
