name: CI
run-name: CI for ${{ github.sha }} on ${{ github.ref_name }}

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
      - "release-**"
      - "track/**"
    paths-ignore:
      - "terraform/**"
  pull_request:
    branches:
      - "*"
    paths-ignore:
      - "terraform/**"

permissions:
  contents: read

jobs:
  openspec-check:
    if: ${{ github.event_name == 'pull_request' }}
    uses: ./.github/workflows/openspec-check.yaml

  tests:
    name: CI
    uses: canonical/identity-team/.github/workflows/charm-pull-request.yaml@be2569198bf90722de218a28abce81786472b83a # v1.16.1
    with:
      base: "24.04"
      k8s-channel: "1.35-strict/stable"
      container-name: "identity-saml-provider"
      use-charmcraftcache: false
      node-size: large
      pytest-model-option: '--juju-model'
      juju-model-name: 'testing'
