name: CI
run-name: CI for ${{ github.sha }} on ${{ github.ref_name }}

on:
  workflow_dispatch:
  push:
    branches:
      - "main"
      - "release-**"
      - "track/**"
    paths-ignore:
      - "terraform/**"
  pull_request:
    branches:
      - "*"
    paths-ignore:
      - "terraform/**"

jobs:
  tests:
    name: CI
    uses: canonical/identity-team/.github/workflows/charm-pull-request.yaml@d64b860c8679d3539f5f35f3d2494c419d8e41b5 # v1.12.2
    with:
      container-name: "identity-saml-provider"
      use-charmcraftcache: true
      node-size: large
      pytest-model-option: '--juju-model'
      juju-model-name: 'testing'
